[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "./prophet-bot"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "prophet-trader"
